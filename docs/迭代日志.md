# pellucid-grove · 迭代日志(活档案)

> **新对话接手必读**。继承自 v1 工程 `docs/v1-迭代日志.md`(归档)。本文件从 v2 工程起步开始记。
>
> **维护规范**: 每次有意义的更新追加一段(模板见 §0)。被改/删的设计也保留并标【废弃】,不直接删。
> **何时不写**: 纯重构/测试适配/类型修正不写;有设计判断的都写。
> **与其它文档关系**: 上层 `../README.md`(工程概览) / `迁移记录-v1到v2.md`(决策史) / `v1-迭代日志.md`(归档参考)

---

## 0. 条目模板

```
## YYYY-MM-DD · 标题 (commit短sha)
**做了什么**: 1-2 句概括
**设计思路**: 为什么这么改,核心判断
**接口/扩展点**: 留了哪些"以后改这里就行"的钩子
**已知限制 / 待补**: 简化的部分、占位、依赖未实现模块
**试玩关注**: 上线试玩时该看哪几个具体点
**下一步建议**: 顺序上下一个该做什么
```

---

## 2026-06-27 · v2 起步 · 迁移 tavern_helper_template + 空壳 build 通过

**做了什么**:
1. v1 工程整体备份到 `../jiutiao-frontend.v1-archived/`(671M·含 .git 历史完整)
2. 原 `jiutiao-frontend/` 工作区清空(保留 .git 接同一 origin pellucid-grove)
3. 复制 `tavern_helper_template` 内容(不含 .git)填充
4. 业务代码迁移: `src/game/` 16 模块 → `src/jiutiao/game/`(纯 TS 零改动);`schema.ts` → `src/jiutiao/schema.ts`(外提一层符合 packaging.md 约定)
5. 修 schema import: `game/schema.smoke.test.ts: './schema'` → `'../schema'`
6. Vue 空壳入口建好: `src/jiutiao/界面/状态栏/{index.ts, index.html, App.vue}`(占位"九条会·前端正在初始化…")
7. package.json: name `jiutiao-frontend` → `pellucid-grove`,version `0.0.0` → `0.2.0`
8. `pnpm-workspace.yaml`(pnpm 自动建): allowBuilds 全 true(`@parcel/watcher` / `unrs-resolver` / `vue-demi`)
9. `pnpm install` + `pnpm build` 通过,产出 **1.5KB 单文件 HTML**(JS 内联,Vue/Pinia 走 testingcf.jsdelivr.net 的 ESM CDN)
10. 改卡侧状态栏正则 HTML: 从 `<iframe>` 改为 `<div id="pellucid-mount">` + `<script>$('#pellucid-mount').load(...)</script>`(带 testingcf → cdn.jsdelivr.net fallback)
11. 文档体系: 工作区根 README + v2 README + v1 归档 README + 迁移记录-v1到v2.md

**设计思路**:
- v1 用 iframe + 自创 postMessage 协议跟酒馆生态不通。试跑暴露 iframe 被酒馆 sanitize 剥离,**致命**(详见 `迁移记录-v1到v2.md`)。
- v2 用 tavern_helper_template 的酒馆生态主流方案:Vue + Webpack 5 输出单文件 HTML + jQuery.load 同源 AJAX 注入。
- jQuery `$('#mount').load(URL)` 同源拿 HTML 文本,**脚本直接操作 DOM 绕过 sanitize**;testingcf.jsdelivr.net 镜像对 .html 返回正确 MIME。
- 业务代码(`game/` 16 模块·纯 TS·262 单测)零改动复用——这是把"两版工程"做成"两层皮"的关键投资,以后再换工程外壳还能复用。
- 部署机制大幅简化: 不再需要 GitHub Pages / gh-pages 分支 / GitHub Actions —— **直接 `git push` 把 dist 推到 master 分支**,jsdelivr 自动同步。
- 锁版本用 commit sha(`@<sha7>`)而非 tag(tag 在 master 但 dist 也在 master,没冲突)。

**接口/扩展点**:
- **业务代码**: 16 个 game 模块对 UI 框架零依赖,可继续用纯函数 + 单测开发。下批 UI 翻译时直接 import。
- **MVU 双向绑定**: 模板内置的 `@util/mvu` 提供 `defineMvuDataStore(Schema, ...)` —— Pinia store 直接绑定 MVU 变量,**免费解决 v1 担心的状态同步问题**。下批 UI 翻译时落地。
- **入口扫描**: webpack 自动扫 `src/**/index.{ts,tsx,js,jsx}` 当 entry,每个 entry 产出独立的单文件 HTML。未来要加新界面(比如设置面板)只需建新 `src/<name>/index.ts` + `index.html` + `App.vue`,自动出 entry。
- **多镜像 fallback**: 状态栏正则 HTML 已内置 testingcf → cdn.jsdelivr.net 回落逻辑,国内网络可靠性提升。

**已知限制 / 待补**:
- **UI 全空壳**: 当前 `App.vue` 只显示一句"九条会·前端正在初始化…",玩家看到这个就证明集成路径通了。真实 UI(行动格分配/状态栏数字/AV 编辑器等)留下批翻译。
- **262 单测未跑**: vitest 配置随 v1 工程废弃,新工程模板没自带 vitest。MVP 阶段先不跑;UI 翻译完后恢复(加 vitest 依赖 + 配置)。
- **business code 没参与 build**: webpack 当前只扫到 `src/jiutiao/界面/状态栏/index.ts` 这个 entry,game/ 的代码没被引用所以没编译。下批写 store.ts 时第一次 import game/ 模块,届时若 import 路径有断会暴露(预计 schema 那次 import 路径调整后没其它问题,但是 v1 的 import 全是相对路径 `./xxx` `../xxx`,新工程拷过来路径没变,理论上 OK)。
- **dist 没 commit**: 当前 `pnpm build` 输出在工作区 `dist/`,但还没 `git add dist/` + push。下一步立刻做。
- **MVU 收尾未做**: v1 时做过 schema.ts → Zod.txt 内联到角色卡脚本,但 schema.ts 现在改位置了,要重新跑收尾。**这个不在当前迁移阶段做** —— 角色卡侧(jiutiao/)的 schema 没变,跟现在工作没冲突。

**试玩关注**:
- 装新卡(`jiutiao/cards/九条会.png`,需 commit dist 后重打)进酒馆
- 选 2.txt 开场白进入
- 应看到酒馆消息下方出现一个 `min-height: 480px` 的暗色 div,几秒后被 Vue 替换为"九条会"标题占位
- F12 → Console → 若失败看 `[pellucid]` 前缀日志(testingcf 失败回落 cdn.jsdelivr.net 的告警)
- F12 → Network → 看到对 `testingcf.jsdelivr.net/gh/wuminggla/pellucid-grove/dist/jiutiao/界面/状态栏/index.html` 的 200 请求 = 集成路径通

**下一步建议**:
1. **本次后续(必须·让卡能跑)**: `git add dist` + `git commit -m "build: v0.2.0 空壳验证"` + `git push` → 重新 pack 卡(jiutiao/cards/九条会.png) → 装卡试跑空壳显示
2. **试跑通过后(下一批·UI 翻译)**:
   - 把 v1 `../jiutiao-frontend.v1-archived/src/hooks/useDayRunner.ts` 翻译成 Pinia store `src/jiutiao/界面/状态栏/store.ts`,接 `defineMvuDataStore(Schema)`
   - 把 v1 `../jiutiao-frontend.v1-archived/src/components/game/GameScreen.tsx` 翻译成 `App.vue`(主界面)
   - 子组件(StatusBar / SavePanel / AvEditor / CharacterPane)逐个翻译成 .vue
   - 业务代码 import 路径调整(如果有断)
3. **UI 翻译完后**: 加 vitest 依赖恢复 262 单测,确保业务代码没在迁移中坏

---

## 2026-06-27 · 集成层踩坑七连(v04→v10)·MVP 空壳上线 (commit 2201a0e)

**做了什么**: 从"卡装上没反应"到"右上角浮动面板渲染九条会标题"·整个集成链路打通。

**v04→v10 决策史**(每版解决前一版的根本性阻塞):

| 版 | 问题 | 解决 |
|---|---|---|
| v04 | 卡里状态栏正则的 `<iframe src=GitHub Pages>` 被酒馆 sanitize 静默剥离;主域 jsdelivr 对 .html 返回 text/plain 浏览器当源码显示 | (尚未解决,只是发现问题) |
| v05 | 改用 `$('#mount').load(URL)` 模式·状态栏正则输出 mount div + script | jQuery.ajax 被酒馆全局加了 `x-csrf-token` header,跨域时 CORS preflight 失败被浏览器阻止 |
| v06 | 改 fetch 替代 jQuery.load,但用户控制台还是显示 v04 内容 | 酒馆助手按脚本 id 缓存,v04→v06 id 没换,内容被认为是同一个不更新 |
| v07 | 每次换 id + 换 projectName(全新角色卡),装新卡看 console | console 完全没 `[pellucid]` 日志,脚本根本没执行 |
| v08 | 查 tavern_helper_template 的脚本示例,发现**酒馆助手 content 字段按 ES module 严格模式执行**,标准模式是 `content="import 'URL'"` 一行,需要 webpack 编译产物部署到 jsdelivr | content 改一行 import,loader 走 webpack 编译输出.[pellucid] 日志终于出来了,但**容器在主页面查不到** |
| v09 | F12 控制台诊断扫所有 iframe → **发现酒馆助手把每个卡脚本放进独立的 `TH-script--<name>--<id>` 隐藏 iframe 沙箱执行**,loader 创建的容器挂到了 iframe 内部 body 而不是主页面.Vue mount 一直成功只是被关在不可见 iframe 里 | loader 改用 `window.top.document.body` 跳出 TH-script sandbox + 创建 srcdoc iframe + 在 srcdoc HTML 注入 jQuery/Vue UMD 让 webpack 编译产物的 `Vue`/`$` 全局可用 |
| v10 | v09 部署后用户控制台还是看到 v08 日志 → jsdelivr 边缘节点缓存了 v08 内容,`?v=v09` query buster 没生效 | URL 改用 `@<commit-sha>/dist/...` (commit sha 锁版本),jsdelivr 对带 sha 的 URL 视为不可变资源永久缓存,但不同 sha 强制回源拉新.**这一招立刻生效** |

**设计思路·最终架构**:
```
酒馆角色卡 (九条会-pellucid-v10.png)
  └─ extensions.tavern_helper.scripts.PellucidLoader
        ├─ id: pellucid-loader-v10-mqvg5d9j  (每次大改换新 id 绕酒馆助手脚本缓存)
        ├─ enabled: true
        └─ content: import 'https://testingcf.jsdelivr.net/gh/wuminggla/pellucid-grove@2201a0e/dist/jiutiao/脚本/PellucidLoader/index.js';
                              ↑ commit sha 锁版本·jsdelivr 永久缓存且不会跟其他 commit 串味

远程 ESM (dist/jiutiao/脚本/PellucidLoader/index.js · 由 webpack 编译输出 · src/jiutiao/脚本/PellucidLoader/index.ts 是源码)
  在 TH-script--PellucidLoader--<id> 隐藏 iframe 里执行
  ↓ 用 window.top.document.body 跳出 sandbox
  在主页面 body 创建可见浮动容器 #pellucid-app-root (fixed right top 460×640px)
  ↓ fetch dist/jiutiao/界面/状态栏/index.html (Vue webpack 编译的单文件 HTML,JS/CSS 内联)
  patchHtml() 在 <head> 注入 jQuery + Vue UMD (远程 HTML 用 const o=Vue / $() 依赖全局)
  ↓ 把 patched html 作为 srcdoc 注入容器内的新 iframe (JS 动态创建,绕过 sanitize)
  iframe 内独立执行 script → Vue mount #app → 渲染 App.vue (当前 = 空壳标题占位)
```

**关键技术点·下次接手必读**:
1. **酒馆助手脚本沙箱机制**: 每个卡脚本放在隐藏 iframe(`TH-script--<name>--<id>`)·内部 `document.body` 不是主页面 body·要操作主页 DOM 必须用 `window.top.document`
2. **sanitize 黑名单**: 状态栏正则 replaceString 输出的 `<iframe>` / `<script>` 标签会被酒馆 sanitize 剥(`<script>` 内容会被当文本暴露).**JS 动态 createElement 创建的 iframe 不受 sanitize 影响**——这是注入 Vue 的合法路径
3. **jsdelivr 缓存反直觉**: `?v=xxx` query string cache buster 在 jsdelivr 边缘节点**不一定生效**.可靠方案是用 commit sha 锁 URL(`@<sha7>/...`)·jsdelivr 视为不可变资源
4. **content 字段标准模式**: 酒馆助手按 ES module 严格模式执行 content·**不能塞 IIFE 源码**(全局 `$`/`Vue` 不可用 → ReferenceError 被静默吞).标准是 `content="import 'jsdelivr_URL'"` 一行·真实逻辑在 webpack 编译后的远程 ESM 里
5. **远程 HTML 的全局依赖**: webpack 编译 Vue 时把 `vue` 当 external 全局(`const o = Vue`).srcdoc iframe 没有 Vue 全局,所以 loader 要 patchHtml() 在 head 注入 jQuery + Vue UMD

**接口/扩展点**:
- `src/jiutiao/脚本/PellucidLoader/index.ts` — loader 源码·改 srcdoc iframe 行为/容器样式/位置都在这里
- `src/jiutiao/界面/状态栏/App.vue` — Vue 主组件·下次 UI 翻译的入口
- 卡里 `jiutiao/脚本/PellucidLoader.txt` — 一行 import URL·**每次推送新远程 loader 后要更新 sha**
- 流程: 改 src → `pnpm build` → `git push` 推到 master → `git rev-parse HEAD | cut -c1-7` 取新 sha → 改 PellucidLoader.txt URL → 改 state.json (PellucidLoader.id + projectName + cardrc artifact) → `pack` → 拖新卡进酒馆

**已知限制 / 待补**:
- **Vue 看不到 chat 上下文**: srcdoc iframe 跟主页面是不同的 origin/window,Vue 没法直接读酒馆 MVU 变量或调酒馆 LLM.下次要做的 `defineMvuDataStore` 集成需要 postMessage 桥或直接走主页面 window 全局.
- **每条 AI 消息都触发状态栏正则**: 但 loader 的 `loadPromise` 单例机制只 fetch 一次,后续消息渲染只会在消息内显示"前端已激活·见浮动面板"指示器,不会重复创建容器.
- **commit sha 锁定意味着发新版要更新卡**: 仅改 dist 不重打卡不会让玩家看到新代码(浏览器/jsdelivr 缓存指向旧 sha).改远程 dist 后必须 `pack` 新版卡并让玩家重拖.或者保留 `@latest`/`?v=`-style URL 用于开发期,正式发布锁 sha.

**试玩关注**:
- 浮动面板出现 = 集成 OK ✓
- "前端正在初始化…"占位 = Vue 在 srcdoc iframe 内正确 mount
- 点"▁"收起按钮折叠 = jQuery 在 srcdoc 内可用
- F12 找 4 条 `[pellucid]` 日志 = loader 执行链路全通

**下一步建议**:
1. **UI 翻译 React→Vue**(task #24): GameScreen.tsx 主界面 → App.vue · StatusBar/SavePanel/AvEditor 等子组件 → Vue · useDayRunner hook → Pinia store
2. **接通 defineMvuDataStore**: 让 Vue store 直接绑定酒馆 MVU 变量(双向反应式)·这是 srcdoc iframe ↔ 主页面 ↔ 酒馆 LLM 的连通点·**最关键的设计**
3. **vitest 恢复 262 单测**: 业务代码 src/jiutiao/game/ 16 模块的纯函数测试·需要补 vitest 依赖配置

---

## 2026-06-27 · v11/v12 推翻 loader 路线·改用原生前端界面机制·数据层全打通 (commit bffd791)

**做了什么**: 子代理深查 tavern_helper_template 后,发现 v04-v10 的 PellucidLoader+srcdoc 整套是远路。改用模板原生"前端界面"机制,数据层探测器五项全绿。

**关键认知翻转**:
- 酒馆助手两种产物: **前端界面**(界面/xxx/index.ts + index.html · 无沙盒 iframe 前台显示 · 酒馆全局开箱即用) vs **脚本**(脚本/xxx/index.ts · 隐藏后台 iframe)。
- 我们 v04-v10 一直把 UI 硬塞"脚本"路线,所以撞 sanitize/CORS/TH-script sandbox 一堆坑,还得自己搭 srcdoc iframe 重造前端界面本就内置的能力。
- **正解**: UI 做成前端界面。`getVariables`/`Mvu`/`generate`/`TavernHelper` 全局直接可用,`defineMvuDataStore` 直连 MVU,状态栏正则用官方 `$('body').load(远程index.html)`。
- v04 的 CORS 错根因是"路线错(用脚本)",不是 `$('body').load` 本身有问题——前端界面在楼层 iframe 上下文执行不带主页 csrf header。

**改动**:
- 界面/状态栏/index.ts → 标准 createApp().use(createPinia()).mount('#app')
- 界面/状态栏/global.css → 九条会暗金配色变量
- 界面/状态栏/App.vue → 数据层探测器(临时·验证前端界面渲染 + 酒馆全局 + MVU 三件事)
- 状态栏正则 → 官方 $('body').load(@sha7 锁版本)
- 删 PellucidLoader 脚本注册 + 卡内 txt(留档 .unused-loader-route)
- 卡脚本现在只剩 MVU

**实战验证(v12 卡)**: 探测器五项全绿——
- 酒馆助手全局(TavernHelper) ✓ / getVariables ✓ / Mvu 全局 ✓ / MVU stat_data ✓ / generate(调LLM) ✓
- 数据层架构确定: 原生前端界面 + 酒馆全局直调 + MVU 双向绑定,零桥接零 postMessage。

**skill 已更正**: ~/.claude/skills/tavern-cards/references/integration-pitfalls.md §0 改为正解(原生前端界面),§1-§7 标注为弯路教训保留。packaging.md §7 占位符更正为"$('body').load 是官方标准做法"。

**待解决(下次注意)**:
- 酒馆当前加载了 3 本世界书(特化大礼包/pixiv黄文文风/九条会0.1),前两本是用户别的预设带的,不影响 stat_data 注入但要确认不污染九条会变量。
- stat_data 顶层键被 MVU 蓝标提示挡住没看清,下次 UI 翻译时直接在界面展示。

**下一步(正式 UI 翻译·task #24)**:
1. App.vue 探测器 → 真实游戏主界面(翻译 v1 GameScreen.tsx)
2. useDayRunner hook(144行) → Pinia store(界面/状态栏/store.ts 旁加 runner store)
3. 子组件 StatusBar/SavePanel/AvEditor/CharacterPane → .vue
4. 业务逻辑直接 import src/jiutiao/game/* 16 模块(纯 TS 零改动)
5. EngineState ↔ MVU 变量: 用 defineMvuDataStore(Schema) 替代 v1 的本地 INITIAL_ENGINE

## 2026-06-27 · 接酒馆主AI + 迁 sillytavern 底座 (commit 952b473·待push)

**做了什么**: UI 翻译后接酒馆 generate 出真实正文。修展开 bug。迁 sillytavern 底座修编译。

**AI 接入架构(子代理查 generate API 后定稿)**:
- 主 AI(正文): `createTavernAi` 调酒馆 `generate({user_input, injects})`,自动套酒馆当前预设/JB/文风/采样参数。游戏范式+态度+状态作为 injects 注入(system 交给酒馆,不重复)。
- 副 AI(抓数值): `generate({should_silence:true})` 静默后台,`extractApi` 留 custom_api 切独立端点(便宜小模型)接口,默认同主端点。
- 零正则污染: 拿 generate 原始返回串,不调 formatAsTavernRegexedString,用户全局正则/扩展不影响事件正文格式。
- 信息流零内容: generate 默认不产生楼层消息(只返回字符串)。
- 文件: tavern-ai.ts(AiPort 实现) + prompt-inject.ts(复用 buildGamePrompt 但空预设,只生成世界书+范式+态度注入)。

**产品方向(用户定·已落实到架构)**:
- 用户游玩完全脱离酒馆原生界面,信息流只渲染一个启动按钮(待做),游玩/正文/存档/留档全在前端。
- 复用酒馆 API 配置 + 预设(JB/文风生效),不自建 API 配置界面。
- 屏蔽用户第三方插件/全局正则(防污染输出格式),只跑我们自己的,留扩展接口。
- 双 API: 主正文/副数值抓取分离;物理上先都走主 generate,副用 custom_api 留切换接口。

**关键修复**:
- 展开 bug: App.vue 的 expandedKey 从非响应式闭包改 ref(`ref<string|null>`),done 格都能展开看正文。
- 编译错误: prompt.ts import ../../sillytavern/{prompt-assembler,types,api-router},v2 只迁了 game/ 没迁 sillytavern。cp v1 整套底座到 src/jiutiao/sillytavern(删 test)。之前编译"成功"是 watch 进程缓存了未编 prompt.ts 的旧状态——教训: build 时确认没有残留 watch 进程。
- runner-store: 默认 createTavernAi(检测酒馆 generate 全局),回落 mock;aiMode/useMock/useTavern 暴露 UI 切换。

**产物**: 52KB→164KB(含 sillytavern 底座+prompt 组装)。should_silence 等逻辑已进 dist。

**已知限制/待补**:
- dexie 未在 v2 package.json(v1 有)。save/transcript 模块依赖它做 IndexedDB,当前可能 tree-shake 没编译或运行时报错。**存档/留档功能要恢复需 pnpm add dexie**。
- 副 AI(抓数值+总结)接口留好但未实战;总结(记忆层 piggyback)未接。
- MVU 双向绑定(defineMvuDataStore)未接,状态还是本地 initialEngine,换楼层/刷新会重置。
- "信息流只留启动按钮"未做(现在整个游戏 UI 直接 load 进楼层)。
- 子组件 AvEditor/SavePanel/CharacterPane 未翻译。

**接手点(网络恢复后第一件事)**:
1. `cd jiutiao-frontend && git push origin master`(推 aecac97 + 952b473,当前因 github 443 超时未推)
2. 取新 sha → 改卡状态栏正则 URL → pack v15
3. 试跑: 状态栏顶部应显示 ◆ 酒馆AI;执行事件格调真 gemini 出按范式扩写的正文(替代 mock 占位)
4. 若 generate 调用报错,F12 看错误(可能 generate 签名/参数要调)

**下一步建议(按用户定的优先级)**:
1. 试跑主 AI 出真实正文(本批)
2. 信息流只留启动按钮 + 屏蔽第三方插件正则(体验骨架)
3. 副 AI 抓数值实战 + MVU 双向绑定
4. UI/交互/视觉 统一改(最后)

## 2026-06-28 · v17 真实正文质量+稳定性(#3#2#4) (commit bb624aa)

**做了什么**: 用户试跑 v16(接真AI能玩)后提 4 个问题,本批改 #3#2#4,#1 留下批。

**用户提的 4 个问题**:
1. (#1)无进度保存,刷新酒馆丢全部进度回退初始菜单 —— **未做**(MVU 持久化是最大工程,下批)
2. (#2)正文混入思维链+尾部格式块(预设要求的数学题等) —— 已改
3. (#3·最严重)AI 没按事件范式生成,而是把上次正文当前文续写(口交供奉格写成便利店采购) —— 已改
4. (#4)空回/截断/卡死无应对,生成中无提示 —— 已改

**#3 根因+解法(核心)**:
- 根因: 用 generate() 套预设,预设有 {{chat_history}} 占位把楼层历史塞进去,我们的 inject depth=0 权重弱被淹没,AI 顺着开场白续写。
- 解法: 改 generateRaw + ordered_prompts 白名单制(子代理查证: 不放的 placeholder 一律不发送)。
  ordered_prompts = [预设JB/main/nsfw块(getPreset读) + 我们的范式注入],**不放 chat_history**。
  prompt-inject 加最高指令: "这是独立事件生成非续写,忽略历史,严禁写成无关场景"。
- 文件: tavern-ai.ts(改 generateRaw) + prompt-inject.ts(加强指令)。

**#2 解法**: 强制 AI 正文包 <jiutiao_text></jiutiao_text>,extract.ts 只提取标签内容。
  标签外的思维链/尾部数学题自动剥离。无标签时兜底剥离 <thinking>/<大写XML>块。

**#4 解法(runner-store + App.vue)**:
- 生成中全屏遮罩 + spinner(防误判卡死)
- 空回检测(AI 正文<20字)→ 橙色警告条 + 重新生成按钮
- AI 超时 120s(withTimeout 包 generateRaw,防卡死拖垮酒馆)
- 重 roll: runCurrent 前快照 day+engine,rerunLast 恢复快照重跑同一格(因 runCurrentSlot 一步到位 completeCurrent 推进 cursor,必须靠快照回退)

**采样**: custom_api same_as_preset 继承预设温度/top_p/max_tokens。

**已知风险/待验证**:
- #2 依赖 AI 听话用 <jiutiao_text> 标签。强势预设 JB 可能不甩,extract 兜底不完美。试跑看效果。
- #3 generateRaw 仍可能带"按深度的世界书条目"(子代理未找到关闭开关)。第三方世界书 depth 条目可能干扰。实测确认。
- 修 SlotCard 展开 bug(task #26)已在 v16 顺手修了(expandedKey 改 ref)。

**下一步(等用户试跑 v17 反馈后)**:
1. 若 #3 未根治 → 加强指令权重 / 查世界书漏入
2. #1 MVU 持久化(defineMvuDataStore·最大工程)+ 正文存档
3. 用户产品方向: 信息流只留启动按钮 / 屏蔽第三方插件正则 / 副AI抓数值实战
4. UI/视觉改版(最后)

**交接**: 因上下文过长,新对话从 ../../jiutiao/README.md 起步。

## 2026-06-28 · v18 连贯性简报代替前文 + 4个体验修复 (commit 5c1fa99)

**做了什么**: 用户试跑 v17(两天流程)后提 5 个新问题,本批改 #1副AI简报/#2供奉降欲/#3请假格分配/#4重生成按钮,#5部分(生成中文案)。

**用户提的 5 个问题**:
1. (#3好转后的新出戏)完全不给前文 → 多格供奉之间无因果关联,反复出现相同描述("凛从外面回到家"连出两格,但前格已在供奉,逻辑矛盾)。**用户定方向:堵不如疏——用副AI总结代替前文。**
2. 重生成上一格按钮在白天/夜晚结束切面时消失。
3. 变量bug:夜晚供奉没切实降低欲望,欲望只增不减。
4. 请假轮奸格分配:白天全空+夜晚8格 → 应白天夜各4格;且待生成/已生成状态区别太小。
5. (UI待办)生成中文案换成有代入感/幽默的"事件演化中""大小姐正在努力"等。

**#1 副AI连贯性简报(核心·堵不如疏)**:
- 架构: tavern-ai.ts 的 createTavernAi 闭包持一个 `recentProse[]` 滚动缓冲(最近3段正文,每段截700字)。每格 expand() 时:①若有前文→副AI(extractApi/should_silence)读最近正文产出"连贯性简报";②简报注入主AI prompt 代替前楼层;③主AI出的有效正文(≥20字)入缓冲供下一格。
- 简报规则(prompt-inject.buildDirectorBriefPrompt): 三段——局面(凛此刻处境,承接最新片段结尾)/衔接锚点(禁止重置的点,如已在供奉就别再写回家)/防重复(已用过的开场/桥段/句式/意象)。
- 主指令(buildGameInject)重写: 从 v17 的"忽略历史·从事件本身起笔"改为"命中范式 + 衔接简报状态不重置 + 只认简报不续写楼层 + 防重复"。这是 v17→v18 的关键认知翻转:v17 为治续写把前文全砍了,反而导致格间断裂;v18 用副AI压缩过的、带规则的简报回补连贯性。
- 简报失败/超时(45s)/开局首格 → 空串,主生成退化为"只看范式"(不阻断)。
- 双AI落地: 主AI(generateRaw正文)+副AI(generateRaw简报,should_silence后台)。这是副AI接口的首次实战(此前只留接口)。

**#2 供奉降欲(economy/settlement)**: 根因 settleNight 只 `desire += desireGain(未供奉)`,被供奉者从不抵扣→只增不减。加 `desireRelief(served)=served×供奉降欲量(2)`,净变化=增-减 clamp≥0。供奉数>未供奉数则欲望净降。UI 夜晚收尾条显示"滚雪球+X / 供奉清欲-Y / 净±Z"(净≤0绿,>0橙)。

**#3 请假轮奸格分配(action-grid)**: buildForcedLeaveDay 从"白天0+夜晚全"改为"白天floor(N/2)+夜晚ceil(N/2)"(8→4/4),两面都 locked供奉,有白天格则 phase=day_running 从白天起逐格。请假在家全天被轮奸,白天也有供奉更合逻辑。

**#4 重生成按钮(runner-store+App)**: 根因按钮只在 *_running 阶段显示,最后一格跑完→phase转 settled→按钮消失。改:canRerun 覆盖 running+settled 四阶段;beginNight 推进时清 lastSettle+快照(防误回退已离开的白天格)。

**#5 部分(生成中文案)**: runner-store 加 genHint(8条随机:事件演化中/大小姐正在努力/时间推移中/...),execCurrentFrom 开始时随机一条,遮罩显示。**格子待生成/已生成视觉区分仍待做**(需 SlotCard 改,留 UI 批次)。

**已知风险/待验证**:
- #1 每格多一次副AI调用(简报),延迟略增(遮罩覆盖)。简报质量依赖副AI听话度;当前副AI=主端点,未切便宜小模型。
- #2 供奉降欲量=2 是起调常数,servedThisNight 可能因 presentCount 累加偏大导致清欲过猛,实测看欲望曲线再调。
- #1 缓冲在内存,刷新丢失(与 #1持久化 是两件事:本 #1=连贯性,持久化仍未做)。

**下一步(等用户试跑 v18 反馈)**:
1. 若简报仍压不住逻辑矛盾/重复 → 调简报 prompt 权重 / 加大 recentProse 窗口
2. MVU 持久化(defineMvuDataStore)+正文存档(刷新不丢进度·最大未做工程)
3. 信息流只留启动按钮 / 屏蔽第三方插件正则
4. UI改版: 格子状态区分 + 全屏窗口 + 黑金视觉

## 2026-06-28 · v19 欲望机制重做+招募即时(item2) (commit 0644256)

**做了什么**: 用户试跑 v18 后,确认 #1连贯性/#2供奉降欲生效,但提出欲望结算"不人性化、不可实时规划"+招募不即时。本批重做欲望生命周期 + 招募即时结算。纯引擎逻辑,不动 schema,与 MVU 持久化批次解耦。

**用户反馈拆解**:
1. (副AI)连贯度改善确认。问实现方式→已解释:每格2次 generateRaw(副AI简报 should_silence + 主AI正文),副AI默认走主端点(extractApi 钩子未接UI)。副api单独端点按钮留到UI批次。
2. 欲望不人性化: 日终才显示降多少,无法实时规划;且日1一直0,玩家不知有欲望要清。**用户要求**: 欲望每天早晨按人数累积(30打手→+30),白天盯着数值规划供奉,日终结算。**关键约束**: 次日晨间累积不参与请假轮奸判定(否则打手>欲望槽永远触发)。
3. 招募不即时: 招募后人数日终才从30→50,要即时。
4. 请假轮奸格4/4(v18已做)/重生成按钮(v18已做)→ 用户指出是已说放待办的UI项,提前做了。已知,不再追加UI投入。

**欲望模型重做(取代 v17/v18 的夜结滚雪球)**:
- 旧: settleNight 把"未供奉×2"加进欲望、被供奉抵扣,日终一次性结算→不可见不可控。
- 新三段(经济模型§7 的 UX 化改写,数值对齐§12百人甜区):
  - **晨间累积**(advanceToNextDay + initialEngine): 每天早晨 `欲望 += 可用打手 × 欲望日增系数(1)`。日1起非0可见,随人数线性增长。`desireAddedThisMorning` 存增量供UI。
  - **实时降欲**(settleServe): 每供奉格当场 `欲望 -= 本场人数 × 供奉降欲量(1)` clamp≥0。状态栏实时掉。
  - **请假判定**(advanceToNextDay): 对"当天供奉后结余欲望" ≥ 上限(60) 判定→次日请假轮奸,**在加次日晨间累积之前**判定(满足用户约束:晨间增量不参与,避免人数>槽永远触发)。
  - settleNight 降级为只读报告(今日供奉数/结余/overflowImminent 预警),不再改欲望/不再置 pendingForcedLeave。
- 数值: 欲望日增系数=1、供奉降欲量=1(v18是2)、阈值=_欲望承载上限=60。可调。

**招募即时(settleRecruit)**: recruit 格执行时当场 `打手 += min(周额度, 买得起的)`,扣钱(招1打手价60·§12.4)、扣额度。周额度 base 5→20(原§11起调5偏慢,上调让手感明显;仍随威望增长)。initialEngine 给初始额度=weeklyRecruitQuota(0)=20。注: 之前 recruit 事件**根本没接经济效果**(settleSlot 不处理),用户看到的+20实为堕落奖励闸门 windfall。本批是首次真正实现招募经济。

**UI**: 分配阶段顶部晨间累积提示条(金色)+招募即时反馈条(绿/警告)+夜晚收尾改"今日供奉X/结余Y/是否将请假"。状态栏欲望实时更新(本就读 engine.desire)。

**MVU 现状澄清(用户问)**: 翻 task_plan/progress 确认。设计层三件套(schema/initvar/更新规则)Phase4已完成;**MVU收尾(卡侧Zod内联/configure)+持久化绑定(defineMvuDataStore)未做**,前置(大前端方向定+schema)已满足,登记为待办#7(独立大工程=真正的"变量统一批次")。item2 不依赖它。

**已知风险/待验证**:
- 晨间累积×1 + 阈值60: 30打手日1晨间+30(<60舒适);需实测人数增长后曲线手感,可调 欲望日增系数/供奉降欲量/阈值。
- settleServe 降欲用 served=presentCount×mult(AI抽取的在场数),与晨间用 thugTotal 基准不同源,极端 presentCount 可能过清/过少;实测看。
- 招募 base 20 是否过快需实测。

**下一步(等用户试跑 v19)**:
1. 欲望/招募手感微调(调常数)
2. MVU 收尾+持久化(待办#7·刷新不丢进度)
3. 产品方向: 信息流只留启动按钮 / 屏蔽第三方正则
4. UI改版(格子状态区分等·最后)

## 2026-06-28 · v20 硬失败双轨+警告 + 修发卡踩坑(第3次) (commit 1e61e84)

**做了什么**: 用户试跑发现 v18/v19 行为(欲望不涨)——查出根因不是代码而是【发卡指向旧sha的踩坑第3次】;同时按用户决定修硬失败。

**⚠️发卡踩坑(第3次·已加护栏)**:
- 现象: 卡明明是 v19,跑的却是 v18 代码(夜晚收尾还是旧文案)。
- 根因: 状态栏正则的**真实源文件**由 state.json 的 `replace_file` 决定 = `正则/🖼️状态栏-显示.txt`,但我一直改的是名字像的孤儿文件 `正则/状态栏界面.html`(state.json 根本没引用它)。每次只改孤儿、pack 用的是另一个文件→卡里 sha 一直停旧版;更坑的是 unpack 验证会用旧内容覆盖回来掩盖问题。
- 修复: 改对文件 + 删孤儿 + **新增唯一可靠护栏: pack 后解码卡内 chara 验证 sha == 新 commit**(不用 unpack 验证)。已写进 ../../jiutiao/README.md 发版流程 step6 + skill integration-pitfalls.md §0.2④⑤。

**硬失败修复(用户决定: 修bug·保留双轨·资金用存量≤0)**:
- 根因澄清: 旧硬失败=`极道威望进账连续2次为0`(威望轨),跟资金无关——所以用户10000块照样硬失败。这是设计补遗_A 故意的"机制即叙事"(极道怕的是名声无法再带来人和钱);用户误以为是资金判定。
- 真bug: ①缺设计要求的"第1次坏审核警告"(完全没实现→玩家莫名其妙死);②资金轨只兜了 money<0 即时,没按设计做。
- 修复(保留双轨):
  - 威望轨: 极道威望进账连续2次为0→硬失败(沿用)。
  - 资金轨: 资金**余额**连续2次≤0→硬失败(用户定用存量·非进账流——"亏一天赚一天"玩家不该被误杀)。新增 economy.auditMoney + EngineState.moneyZeroStreak。
  - **第1次坏审核弹红色警告**(任一轨·留1回合缓冲): "威望停滞…招牌要烂了"/"资金见底…现金流要断了"。
  - DailySettleResult 加 hardFailReason/failWarnings;硬失败文案区分威望/资金轨。

**已知风险**: 威望轨仍会因"2天不打架"硬失败(设计如此·用户要保留),现在有第1次警告兜底;实测嫌严可加开局宽限。v20 卡=九条会-pellucid-v20.png @1e61e84(解码验证 sha 正确)。**v19 作废**(那张卡 sha 指错)。

**下一步(等用户试跑 v20)**: ①欲望晨间累积/实时降欲/招募即时这次才真能测到 ②硬失败警告+双轨手感 ③数值微调 ④MVU持久化(#7)/产品方向/UI改版

## 2026-06-29 · v21-v22 试跑微调 + 全新UI落地 (commit 680ed62)

**v21-v22(快修)**: 事件堕落度降到1-4/次(原5-20·治堕落奖励暴塞打手死档)；招募周额度基础→10、单格招募3-4浮动；威望轨硬失败暂挂(地盘玩法未接入前不罚·`settlement.ts 威望轨硬失败已启用=false`)；避孕套采购即时结算；范式禁跨时段；欲望警告仅超上限弹。卡 v21=@5e8c45d / v22=@2c45878。

**v23 全新UI落地(本批·大改)**: 用户选定「极道手账·木纹②」版式，从 HTML 空壳原型翻成 Vue 组件接真逻辑。
- **设计系统**: `界面/状态栏/global.css` 集中 tokens(黑金红白配色/云峰飞云体+京华老宋体 zeoseven CDN/木纹②/融合滚动条/青海波底纹)。改风格只动 :root。
- **组件化**: `App.vue`(外壳 Grid 三列两行+「行动」视图编排) / `Masthead.vue`(顶栏6数值+悬停浮窗+点击钉住·威望双色条) / `NavRail.vue`(镶嵌式木纹导航+设置/退出) / `DaySlider.vue`(日夜分配滑动长条·整格吸附) / `RinPanel.vue`(立绘+可折叠秘密状态·身体开发四部位) / `SlotCard.vue`(重制样式·逻辑不变)。
- **数据**: 接 runner-store 真逻辑；威望(martial/infamy双色条)/打手在场/欲望/避孕套/堕落度+认知防线下阶段奖励 都接真值；🟡占位=资金流水(#12)/分部位次数/性癖觉醒度/立绘图(见 `docs/UI改版工程说明.md` §3 全表)。
- **导航页**: 仅「行动」完整可玩，地盘/升级/影业/大小姐/留档/设置 占位(待 #11~#15 接入)。
- 旧 StatusBar.vue 废弃留档；旧朴素 App.vue 被替换，业务调用原样复用。

**工程说明**: 新增 `docs/UI改版工程说明.md`(架构/组件职责/数据映射/占位清单/改动指引)——后期改 UI 必读。

**发版踩坑(本批)**: `pnpm build` 因 template_sync 改了 lockfile 触发 pnpm 供应链策略(minimumReleaseAge)拦 install。绕法: node_modules 已在,直接 `node node_modules/webpack/bin/webpack.js --mode production` 跳过 pnpm install。卡 v23=九条会-pellucid-v23.png @680ed62(解码验证✓)。

**下一步**: 用户试跑 v23 看新 UI 整体观感/可玩性 → 反馈后细调 → 逐个接入占位面板(地盘#13/升级/AV/资金流水#12/威望显示完善#11)与 MVU 持久化(#7)。

## 2026-06-29 · v24 信息流只留启动按钮 + 全屏展开 (commit 75375be)

**做了什么**: v23 新 UI 被挤进窄窄的楼层消息列(整套 3 列布局塞进消息宽度→糊成一团)。落实产品方向"信息流只留启动按钮，点击全屏展开"。

**机制(关键)**:
- 楼层 iframe 里只渲染一个金色【展开·九条会】按钮(index.ts 直接 createElement，不挂 Vue)。
- 点击 → 把 App 挂到酒馆【顶层窗口 window.top.document】的 fixed 全屏宿主(z-index 极高)覆盖整个酒馆。**前端 JS 仍跑在楼层 iframe 上下文**(generate/Mvu/getVariables 全局照常可用)，只是 DOM 提到顶层全屏显示。
- 顶层右上角加【✕ 收起】按钮(隐藏宿主回酒馆，App 不卸载、进度保留)。
- 退化: 拿不到 window.top(跨域/异常)→ 回退楼层内直接挂载(虽挤但可用)。

**配套改动·样式作用域收敛(必须)**:
- 因为要把本文档 <style> 克隆进酒馆顶层 <head>，global.css 里**绝不能有裸 `*`/`body`/`#app` 全局规则**(会污染酒馆本体)。已把全局重置/底色/纹样/滚动条全部收敛到 `.pellucid-root` 作用域；:root 变量保留全局(无害)。App 根 `.app` 加 `.pellucid-root`。
- 青海波纹样从 ::before 改成 .pellucid-root 的 background-image(避免根元素层叠问题)。

**发版**: 同样用 `node node_modules/webpack/bin/webpack.js --mode production` 绕过 pnpm install 供应链拦截。卡 v24=九条会-pellucid-v24.png @75375be(解码验证✓)。

**下一步**: 试跑 v24 看全屏展开观感 → 后续可加"可调窗口"(拖拽/缩放，现在是纯全屏) + 屏蔽第三方插件正则 + 逐个接占位面板。

## 2026-06-29 · v25-v28 行动视图重构 + 横条交互 + 版本号 (commit 778876e)

- **v25**: 修日夜滑条拖不动(全屏后 DOM 在顶层窗口,拖动监听原挂在楼层 iframe 的 window→收不到顶层指针事件;改 setPointerCapture+元素级监听·跨文档可靠) + 删独立收起按钮·收起接到左栏「退出」(index.ts provide pellucidCollapse 给 App)。
- **v26**: 行动视图重构(用户新布局思路·减少上下滚动): 8格事件横条 SlotStrip(对齐滑条) + 选中格子页 SlotDetail(事件选择/正文) + 固定底边栏(左 status-strip 状态提示·右操作按钮)。修正文段落排版(white-space:pre-wrap)。
- **v27**: 自动跳转做成开关(快进+自动跳转→2个带 title 说明的开关按钮);已结算格加「✓ 展开查看」;序号选事件后淡化成背景水印(腾出格子空间);滑条分配完用 `<Transition collapse>` 折叠淡出(非瞬间消失)。
- **v28·版本号**: 用户反馈"v27卡打开是v26"。**排查结论: 卡正确·非发版坑**——卡 chara+ccv3 两块都 @045cd9f,且 curl jsdelivr @045cd9f 实测字节与本地 dist 完全一致、含 v27 标记。判定是加载侧(旧聊天的旧正则/缓存)。**对策**: `src/jiutiao/界面/状态栏/version.ts` 加可见构建版本号(刊头副标题「极道手账 · vXX」+ 控制台 `[pellucid] 构建版本`)。每次 pack 前同步改 version.ts。这样能一眼确认加载版本,终结"到底哪版"的扯皮。

**发版校验升级**: 解码卡内 sha 后,再 `curl jsdelivr @<sha> | wc -c` 与本地 dist 字节比对(本批已验证 211090 字节一致),彻底排除 jsdelivr 侧问题。卡 v28=九条会-pellucid-v28.png @778876e。

## 2026-06-29 · v29 MVU持久化(刷新不丢进度) (commit facd4ec)

**做了什么**: 交接里的"最大未做工程"——之前状态全在前端内存,刷新酒馆/重开聊天=进度清零回初始菜单。本批落地持久化。

**方案(用酒馆"聊天变量"·非 defineMvuDataStore)**:
- 研究后发现酒馆变量有 `chat` 作用域(一聊天一份·跨楼层持久·刷新不丢),比 defineMvuDataStore 的 message+schema 绑定更简单可控。
- runner-store 加: `snapshot()`(JSON克隆 engine+day+fastForward) / `persistNow()`(insertOrAssignVariables 写 chat 变量 `九条会存档`) / `loadSave()`(getVariables 读回) / `schedulePersist()`(400ms防抖) / `resetGame()`。
- 启动: 有存档→读回; 无→落初始并写一次。`watch([day,engine,fastForward],deep)` → 任意状态变化自动防抖存。
- **已生成正文随 day.slots[].resultText 一起持久**(展开已结算格仍能看正文)。
- 环境守卫: `hasTavernVars`(无 getVariables/insertOrAssignVariables 的本地 mock 环境跳过,不报错)。
- 左栏「存/读档」按钮=手动立即存档(给确认 toast);进度本就自动存。新游戏=新开聊天(空 chat 变量→初始)或 resetGame()。

**未做(本任务的另一半·可能已不需要)**: schema.ts → Zod 内联到卡脚本的"MVU收尾"是为了让 AI/世界书经 stat_data 蓝标读变量。但我们 generateRaw 是把 state 显式注入(不读 MVU stat_data),所以这条对当前架构非必需;留作以后若要"AI 直接感知变量"再做。

**卡 v29**=九条会-pellucid-v29.png @facd4ec(解码两块+curl字节 212972 双验证✓)。试跑: 玩几格→刷新酒馆/重进聊天→应停在原进度(非回初始)。
